# QM Central Management —— 中央物资管理（1.6.1）

## 已实现

- 特工装备展开时，左侧装备栏上方增加“装备预设”工作台；预设可自行命名、覆盖保存、下拉选择、删除并一键应用。
- 一个预设会保存当前武器、防具、背包/弹挂等装备栏位、背包与弹挂/快捷栏中的全部普通物品及准确数量，以及机械替换部件、实际义体插槽容量和已安装义体。
- 应用前会先扫描全部中央库存并检查科技、增强体部门、身体兼容性、义体插槽容量、锁定装备和可用栏位。只有物品数量不足时可选择强制应用：缺少的普通装备栏位留空、缺少的携带物品跳过；身体配置不完整时保留当前身体。结构或锁定冲突仍会阻止应用。
- 换装顺序固定为“收回冲突装备 → 调整机械部件 → 安装义体 → 穿戴装备”。已经符合预设且插槽容量足够的机械部件会原样保留，只替换真正不匹配的部位。
- 预设以物品类型为目标，而非绑定保存时的某一件物品；仓库有多个同类物品时优先选择未损坏、耐久更高且满足义体插槽要求的实例，因此可以跨特工复用。
- 在供应科技树的“回收 / 拆解模块”正下方增加大型“中央物资管理”科技。
- 解锁后，在飞船界面右上角的干员、库存按钮左侧增加中央物资管理入口。
- 同时索引 1～7 号库存、已建造的冷藏区，以及空闲回收区中的产物。
- 按本地化名称或内部 ID 搜索；支持大类和细分类筛选，包括远程/近战以及手枪、霰弹枪、冲锋枪、步枪和重武器。
- 按物品种类聚合显示；装备栏展开时每页 15 类，收起时每页 30 类。卡片显示名称、库存总数量和已选回收数量。
- 卡片中的图标是原物品格：可拖到左侧干员装备/背包；把左侧物品拖到中央面板空白处即可入库。
- 左键点击物品卡片的“全选”会立即全选/取消该种物品，不再弹出数量窗口；右键“全选”才打开精确数量面板，可输入数量或使用 ±1、±10、全部。
- “全选当前”是切换按钮：当前搜索/分类结果尚未全选时一键全选，已经全选时显示“取消当前”并一键取消。
- 右键物品图标继续使用游戏本体物品菜单。对堆叠物品执行原生“拆分”并确认后，鼠标会立刻拿起刚拆出的那一叠，无需再点或再拖一次。
- 精确数量窗口支持 Enter 确定、Esc 取消；弹药、食物等堆叠物品仍会按指定单位数安全拆分后送入回收舱。
- 鼠标位于中央管理面板内时，滚轮可直接前后翻页；打开游戏本体的物品拆分滑块后，滚轮每格调整 1 个。
- 搜索栏右侧增加动态排序菜单：普通物品可按名称或数量排序；装备“全部”会严格按游戏 `ArmorSets` 正式配置聚拢套装，也可和头盔、护甲、护腿、靴子一样按总抗性或各伤害类型抗性降序排列；武器可按总伤害强度或具体伤害类型排序，弹药按对应伤害类型与伤害倍率强度排序。切换同一大类的细分类时保留当前排序。
- 植入体分类增加动态“部位”二级筛选，直接读取植入体的安装槽位；生体植入物也会通过其伤口槽记录识别可安装部位。
- 排序菜单新增“科技等级”，读取物品自身的 `ItemRecord.TechLevel`，在所有分类下都可用（与“名称”“数量”并列），按科技等级从高到低排列。
- 底栏新增“维修”按钮：一键维修**当前选中特工身上**的全部受损装备，走的是游戏本体的维修逻辑，所以兼容规则、耐久倍率和耗材消耗完全与手动拖拽一致。
  - **维修范围**是该特工的 `Inventory.AllContainers`——身上穿戴的全部装备栏，以及背包和弹挂里的物品，也就是面板左侧特工面板显示的那些。未选中特工时按钮不可用。
  - **耗材只从飞船库存出**（1～7 号仓与冷藏区）。回收舱不动（那些本来就要销毁），**特工自己携带的维修包也不会被消耗**——那是准备带去出战的，在基地悄悄烧掉只会帮倒忙；想用就手动拖。
  - **优先消耗维修包，其次才是零件**：维修包（`MaxCapacity` 为正）会返还一部分永久耐久上限，而破布、弹簧、装甲板这类零件（`MaxCapacity` 为负）是拿永久上限换当前耐久。随手抓一个匹配的会悄悄把好装备磨废。
  - **绝不会把任何东西修报废**：原版是先扣耐久再判断上限惩罚是否满 100%，满了就把物品变成废铁；批量操作不能替玩家做这个决定，所以会修到报废的那一次维修不会执行（惩罚值按当前特工的耐久倍率换算后再判断，与原版一致）。
  - 与原版手动维修等价：每次成功维修都会照常触发维修相关的技能经验，修完后重算该特工的抗性（因为身上的护甲状态变了）。
  - 执行前有确认对话框（复用原版对话框，与批量回收一致），执行后弹出结算清单，列出实际消耗了哪些耗材、各多少个。
- 飞船入口改为中央管理专用横向按钮，不再克隆原版货舱按钮；默认快捷键为 `C`，可在 `config.txt` 中通过 `shortcutKey` 修改或设为 `None` 关闭。
- 任务简报界面（选好任务和干员的那一屏）左下角增加中央物资管理入口，与右下角“开始行动”同一行，同一个 `C` 快捷键也可用。它直接以简报里已选中的干员打开面板，并默认展开该干员的装备栏，关闭后回到简报（装备重量随之刷新）。出现条件与原版“选择装备”按钮一致：已解锁科技并已选定干员；可用 `raidPrepCentral` 关闭。

## 空间站贸易面板

- 替换原版空间站交易界面（需解锁“中央物资管理”科技；游戏“快速交易”选项需开启）。
- 买卖同屏：购买页列出空间站全部存货（含消耗品，卖出后可买回）；出售页只显示空间站会收的物品，我方全部仓库自动汇总；支持搜索、分类筛选与名称/单价/数量排序。
- 列头标明数字含义；购买页底部显示原版折扣/加价条；价格完全沿用原版公式。
- 出售与购买共用一个购物车，一键“交易”先卖后买；底栏联动显示收入、支出与交易后余额；`tradeConfirm` 可开启二次确认。
- 滚轮只翻页；按住左键扫选（− 减 / + 加 / MAX 拉满；Shift/Ctrl/Ctrl+Shift ×10/×100/×1000）。
- 快捷键：B 切换买卖页、[ ] 翻页、D 交易、Delete 清空购物车、A 全选可见。
- 以物换物可见化：交付任务物品（如安共数据芯片）后弹出清单，列出空间站交还的物品与数量（自动入仓）。
- 切换游戏语言时面板自动重建。
- 可选兼容 Traveler 的 Global Currency：检测到该 mod 后，余额显示与购买判断改读 Magnum 全球货币；没装时行为不变。尊重对方 MCM 里「Public Enemy 不用全球货币」的选项。

## Mod 设置菜单（MCM）

- 可选集成 Crynano Mod Configuration Menu：订阅后可在游戏内 Mod 设置页直接修改 `raidPrepCentral`、`shuttleManifests`、`shuttleAutoRestock`、`stationTrade`、`tradeConfirm`、`autoUnlockTech`、`debugTradeLayout`，保存立即生效并写入 `mcm_settings.txt`（未订阅 MCM 时也生效）。
- MCM 条目随游戏语言显示中文/英文。

## 批量回收保护

- 必须再次点击确认。
- 回收舱工作时禁用。
- 自动排除锁定物品、任务物品、隐式武器、损坏且不可拆解的物品，以及已经在回收舱里的物品。
- 搬运前后检查全部库存的物品堆总数，异常会写入 `Player.log`。

## 版本记录
- 1.6.1 修复装备预设下拉只看得见最后存的两条：预设都在，列表被装备窗挡住并卡在两行高的条里；现已抬到上层，超过 8 条可滚轮。
- 1.6.0 修复中央面板翻页：最后一页页码少一页、且往回翻会跳页并偏移（最后一屏贴底对齐，而翻页原本是位置累加；现改为按页索引驱动）。穿梭机补给清单：把货运穿梭机货舱存成具名清单并一键补满，可在出战瞬间自动补齐（只补缺口，不拿走已放入的东西）；出战准备界面的“货运穿梭机”页签与中央面板均可操作。结算界面新增装备预设栏（应用即“有选择的卸载”）。中央面板右上角改为下拉菜单（特工装备 / 穿梭机 / 安装义体 / 收起），穿梭机页直接使用原版货舱格子。以物换物结算改为带物品图标的列表。新增“投掷部署”分类（手雷、地雷、炮台、可放置掩体），两个面板分类统一。修复模组弹窗点击自身空白处会关闭的问题。预设文件格式升至 V4。兼容补丁：空间站贸易面板识别 Traveler 的 Global Currency，余额与购买判断改读 Magnum 全球货币（不升版本号）。
- 1.5.1 贸易面板改为居中并按实际设计空间自适应：修复超宽屏（21:9 / 32:9）下面板被钉在左上角的问题——游戏按宽高比切换 UI 缩放模式，超宽屏的设计空间是变宽而非变矮。宽度铺满画布（设上限）、行数按可用高度拟合，靠右控件整体跟随右边缘；4:3 / 5:4 每页 18～19 行；16:9 布局与 1.5.0 逐像素一致；游戏内改分辨率即时重排；贸易页脚不再每帧重拼结算文字。
- 1.5.0 空间站贸易面板：替换原版交易界面，买卖同屏、购物车合并交易、扫选与快捷键、以物换物清单弹窗；价格沿用原版公式。可选集成 Mod Configuration Menu（游戏内设置）；autoUnlockTech 对新建存档生效；修复英文界面下 Mod 设置显示中文。
- 1.4.1 安装义体界面自动隐藏出发前预设栏，不再顶偏装备界面；出发前装备界面的一键应用预设现在包含义体 / 机械臂，同样做缺失、锁定与义体部门校验；保存与应用预设的弹窗文案与中央面板统一。
- 1.4.0 分页改为连续滚动（滚轮与箭头整屏移动）；整张卡片可点击，Shift 支持范围多选；右键改用原版带数量滑条的上下文菜单；批量回收改用原版确认对话框；义体子分类精简为“肢体 / 义体”，切换时保留已选身体部位；筛选无结果时新增“清除筛选”；修复面板内按数字键 1-9 会切换底层界面页签、关闭按钮溢出面板、窄屏页脚统计与按钮重叠、Shift 锚点在手动取消选择后残留；搜索框聚焦不再屏蔽全游戏按键，仅屏蔽输入框实际会用到的键。装备预设栏移至出发前装备界面（库存窗口上方、同宽、自动避让标签条），开局无需科技解锁，中央面板预设保留；预设应用不再把物品送入未建部门的冷藏/回收舱；修复屏幕销毁时弹窗遮罩泄漏。
- 1.3.1 修复从中央面板安装整条手臂 / 腿等机械肢体后增强体界面不刷新：消除对原版装备视图的重复事件订阅（空引用异常的根源），并给原版重量面板加空值保护；替换肢体时旧部位移除、新部位安装现在会完整走完。
- 1.3.0 界面按游戏本体素材重制：九宫格边框、按钮三态贴图、原版点击音效与字号；卡片改为名称独占整行、宽屏 4 列，分类页签自动分行并撑满；修复装备预设无法存档（改为自行读写的行格式，不再依赖 Unity JsonUtility）；修复机械肢体被归类到武器栏；义体新增“肢体/义体”子筛选且切换时保留部位选择。
- 1.2.0 装备预设现会保存背包与弹挂/快捷栏的全部物品和数量；物资不足时可强制应用现有部分，缺少的普通装备栏位留空，身体配置不足则保持原状；补齐原版按钮、装备与拿取音效。旧预设仍兼容，且不会误清空旧格式未记录的背包。
- 1.1.0 新增完整装备预设：自定义名称、保存/覆盖/选择/删除、一键仓库预检与自动换装，并覆盖武器、防具、机械替换部件及义体。
- 1.0.0 首次创意工坊正式发布；包含下列全部中央库存、特工装备、义体安装、排序筛选和批量回收功能及修复。
- 0.1.23 修复从增强体界面点击“特工装备”后左侧装备栏立刻消失：游戏会在 `Configure` 和屏幕 `OnEnable` 两个阶段连续初始化中央面板，第二次初始化曾把第一次的展开状态重置；现在同一次界面激活只初始化一次状态，后续启用回调只恢复当前布局。
- 0.1.24 修复特工装备与增强体界面切换时子分类栏闪烁：子分类按钮现在从第一次布局起就按当前分类的实际数量等宽排列，面板也会在标签、选中状态和布局全部刷新完毕后再显示。
- 0.1.25 统一修复浮层关闭时的鼠标穿透：排序、义体部位、特工选择和数量弹窗关闭后都会吞掉本次鼠标释放、暂停原版拖拽控制器，并阻止中央物品快捷点击收到同一事件。
- 0.1.26 修复从增强体界面返回“特工装备”时中央库存被强制切换到“义体”分类：展开特工装备与选择中央库存分类现已拆分为两个独立状态；返回后展开左侧装备栏，右侧从普通“全部”开始。

- 0.1.22 将增强体模式的“收起义体”改为“特工装备”，点击后返回中央装备界面并自动展开左侧特工背包/装备栏；装备“全部”开放总抗性与各类抗性排序，且切换装备、武器、弹药细分类时不再重置为名称排序；武器新增按平均基础伤害和钝击、穿刺、切割、火焰、寒冷、毒素、电击、光束、爆炸、等离子、混沌、质子、冷冻伤害类型强度排序，弹药对应按实际伤害倍率排序。

- 0.1.21 将全仓库扫描、同类物品聚合、代表堆叠选择及官方套装关系读取抽离为独立索引层，降低中央 UI 与物品数据的耦合；植入体分类下的“安装义体”按钮可直接切换到当前特工的原版增强体人体面板，中央聚合库存继续显示在右侧。右键中央物品图标即可使用原版“增强/植入”命令，安装限制、替换旧义体、物品消耗和属性刷新全部沿用游戏本体逻辑；“收起义体”可返回中央管理，特工下拉切换在增强体模式下同样生效。

- 0.1.20 将飞船入口主体内缩到原版按钮的 20×20 内容区，保留四周留白与单层原版边框；新增与原版一致的三态像素资源，悬停时图标变黄、按下时图标变暗，不再用同一张图片覆盖所有交互状态。
- 0.1.19 直接核对游戏 `SpaceshipScreen` 资源中的真实预制结构：上方功能图是根对象 `CommonButton.background`，下方快捷键位于独立的 `Hint/GameKeyPanel`。现在只替换根背景为中央管理图标，并让原版热键组件显示 `C`，不再遍历或误改底栏图片。
- 0.1.18 修正飞船入口图层识别：上半格只显示中央物资管理图标，下半格直接复用原版 `HotkeyIcon` 显示 `C`，不再把中央图标误塞进快捷键栏。
- 0.1.17 不再估算或手绘飞船入口几何结构，直接复用原版 `M` 按钮的完整预制层级，仅替换中央管理图标、快捷键和提示；入口位置始终用 `C = M - (A - M)` 计算，确保三格同尺寸、同基线、严格等距。
- 0.1.16 将飞船入口改为与原版 `M/A` 完全同宽同高的方形按钮，紧贴在两者左侧；入口仍使用独立内框、中央管理图标与底部 `C` 快捷键栏，不再显示突兀的横向文字条。
- 0.1.15 将套装关系改为游戏官方 `ArmorSetRecord.Items`，删除内部 ID/名称猜测；没有正式套装关系时隐藏套装排序。飞船入口重做为独立横向 UI，并增加可配置快捷键。
- 0.1.14 增加面板滚轮翻页、原版拆分窗口滚轮调数量、装备情境排序、套装聚拢及植入体安装部位二级筛选。
- 0.1.13 同步中央卡片物品格的最终位置与游戏本体悬停缓存，修复鼠标移出物品图标时图标向左跳动的问题；归还对象池时也会恢复该缓存。
- 0.1.12 将原生拆分结果直接交给游戏本体拖拽器：确认拆分 4 个后，鼠标立即拿起 4 个；同时冻结弹窗关闭时的鼠标松开事件，修复点击数量窗口“确定”会穿透并顺势点击下方物品的问题。
- 0.1.11 将卡片回收选择拆成“左键快速全选/取消、右键精确数量”，把底部“全选当前”改为可反向取消的切换按钮；接入游戏本体右键拆分结果，使拆出的那一叠成为中央卡片下一次拖动的目标，并在全屏布局底部显示操作提示。
- 0.1.10 将批量回收从“按物品种类全选”改为“按单位数量选择”；卡片显示已选单位数，底部按单位总数确认，并使用游戏本体的堆栈拆分规则处理部分弹药、食物和消耗品。

- 0.1.6 首次打开时优先使用最近一次出战的可用特工；特工标题改为角色实例姓名，并可点击姓名从下拉菜单直接选择，不必逐个翻页。
- 0.1.7 修复特工下拉菜单点击层与姓名文本冲突、导致中央管理界面只剩黑色底板的问题。
- 0.1.8 特工标题和下拉菜单改为游戏本体的正式本地化姓名，不再误用随机行动代号。
- 0.1.9 特工姓名改为与游戏本体“管理操作员”列表完全相同的 `spec.<profile>.name`，修复误显示角色称号的问题。
- 中央聚合卡片增加 Ctrl 点击节流：一次 Ctrl+鼠标左键只转移当前一堆，避免与 DragSweep 类批量拖放 mod 组合时因刷新卡片而连续取走多堆；其他仓库界面的批量拖放行为不变。
- 0.1.5 整体增大标题、搜索、筛选按钮、物品名称、数量和底栏字号；物品卡片只显示名称与库存总数，不再显示“堆叠组数 / 所在仓库”等内部信息。
- 0.1.4 使用两套动态布局：展开特工装备时中央面板占满右侧剩余区域；收起装备后自动扩展到几乎整个 640×360 逻辑画布，筛选栏、卡片列数和分页容量同步重排。
- 0.1.3 将中央管理改为独立的 282×202 右侧大面板；原版货舱只会被临时隐藏，不再被复制、缩放或参与中央面板布局。
- 特工装备栏默认收起，通过标题栏“特工装备”按钮展开；展开后与中央管理并排显示，可直接拖放装备和背包物品。
- 修复中央物品格污染全局 UI 对象池的问题。中央面板持有自己的专用物品格，销毁时按原版 `ItemSlot` 参数完整复原后才归还，不再破坏原仓库排列。
- 0.1.2 不再按整个屏幕猜测尺寸，而是直接占用原版右侧库存窗口的 236×150 逻辑区域；关闭中央管理时会显式恢复所有原版窗口状态。
- 标题栏增加左右特工切换按钮，可在非克隆、非出任务状态的特工背包之间切换；切换时同步更新原版拖放控制器和物品提示上下文。
- 中央入口不再显示从原库存按钮继承来的 `A` 快捷键，以免造成误导。
- 0.1.1 将面板改成贴靠屏幕右侧的三列紧凑布局，避免游戏 Canvas 缩放后越过屏幕边缘。
- 飞船入口及科技名称统一为“中央物资管理”，不再继承原库存按钮的“货舱”提示。
- 科技节点使用 38×38 大型科技图标，并按照大型升级处理。
- 从飞船中央入口打开时，左侧默认使用第一名处于空闲状态的干员；没有空闲干员时仍可管理物资，但不显示干员库存。
- 聚合卡片每次拖出数量最大的一堆；拖出后会自动刷新为同类物品的下一堆。
- `F` 表示冷藏舱，`R` 表示回收舱。

## 配置

`config.txt`：

- `debugLogging=false`：详细日志。
- `recycleConfirmSeconds=4`：已废弃（批量回收改由原版确认对话框负责），仅为兼容旧配置保留。
- `shortcutKey=C`：飞船主界面与任务简报界面的快捷键，使用 Unity `KeyCode` 名称；设为 `None` 可关闭。
- `raidPrepCentral=true`：任务简报界面的中央物资管理入口按钮。
- `shipLoadouts=true`：出战准备装备界面上方的装备预设条（无需解锁科技）；设为 false 完全隐藏。
- `loadoutBarOffsetY=0`：装备预设条的垂直微调（画布单位），正值远离装备窗口。
- `shuttleManifests=true`：穿梭机补给清单（预设条在「货运穿梭机」页签下切换为清单控件；需要 `shipLoadouts=true` 与货运穿梭机科技）。
- `shuttleAutoRestock=true`：按所选清单自动补满穿梭机（只补缺口，从不拿走东西）。
- `stationTrade=true`：是否用本模组替换原版空间站交易界面（门槛 = 本开关 + 已解锁“中央物资管理”科技）。
- `autoUnlockTech=false`：每个存档（含新建）自动解锁“中央物资管理”科技。
- `tradeConfirm=false`：整笔交易前二次确认。
- `debugTradeLayout=false`：打开贸易面板时把原版界面层级写入 Player.log（诊断用）。
- `quantityShiftStep=10` / `quantityCtrlStep=100` / `quantityCtrlShiftStep=1000`：Shift / Ctrl / Ctrl+Shift 点击与扫选的步进。
- `shortcutTogglePane=B`、`shortcutPrevPage=LeftBracket`、`shortcutNextPage=RightBracket`、`shortcutTrade=D`、`shortcutClearCart=Delete`、`shortcutSelectAll=A`：贸易面板快捷键（`None` 关闭）。

已订阅 Crynano Mod Configuration Menu 时，`raidPrepCentral`、`shuttleManifests`、`shuttleAutoRestock`、`stationTrade`、`tradeConfirm`、`autoUnlockTech`、`debugTradeLayout` 可直接在游戏内 Mod 设置页修改，保存后立即生效并写入 `mcm_settings.txt`。
